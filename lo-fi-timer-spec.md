# 深度访谈规格：Lo-fi 复古网页计时器

## 元信息
- 轮次：6
- 项目类型：绿地
- 最终歧义：16.1%
- 阈值：20%（标准模式）
- 状态：通过阈值 · 待审批

## 清晰度拆解
| 维度 | 分数 | 权重 | 加权 |
|---|---:|---:|---:|
| 目标 | 0.95 | 0.40 | 0.380 |
| 约束 | 0.75 | 0.30 | 0.225 |
| 成功标准 | 0.78 | 0.30 | 0.234 |

## 拓扑
| # | 组件 | 状态 | 描述 |
|---|------|------|------|
| 1 | 视觉外壳 | 进行中 | 矩形窗口容器+标题栏+边框凸起凹陷质感，lo-fi复古感，暖米色低饱和色调，背景雨夜窗台GIF |
| 2 | 计时器核心 | 进行中 | 倒计时显示（细衬线数字）、开始/暂停/重置；多任务排队，每任务独立时长，队列跑完停在0:00 |
| 3 | 待办输入 | 进行中 | 任务名+时长输入，添加到队列，队列可管理 |
| 4 | 音效反馈 | 进行中 | 倒计时归零播放轻柔颂钵音效 |
| 5 | 数据持久化 | 进行中 | 自动保存每日专注记录到 localStorage，抽屉侧栏可回看今日记录 |
| 6 | 交互细节 | 进行中 | 按钮hover微弱发光，无刺眼高饱和色彩 |

## 目标
构建一个单HTML文件的lo-fi复古专注计时器。用户打开页面后：输入专注任务名和时长 → 添加到任务队列 → 点击开始 → 逐任务倒计时 → 每任务完成播放颂钵音效 → 队列全部跑完静默停在最后一个任务的0:00。所有专注记录自动按日期存储，通过侧边抽屉可回看今日记录。

## 约束
- 单HTML文件，双击浏览器即可打开，无构建工具依赖
- 雨夜窗台背景GIF和颂钵音效通过网络资源获取内嵌（data URI或CDN直链）
- 暖米色/奶油色低饱和配色，无高饱和、无刺眼荧光色
- Windows 95 复古风格只取"矩形窗口+标题栏+边框凸起凹陷质感"，不照搬原始配色
- 细衬线字体（serif）显示计时数字
- 按钮hover微弱发光效果（box-shadow glow，低透明度暖色）
- localStorage 存储每日记录，按日期分组
- 无后端，纯前端静态页面
- 无emoji

## 非目标
- 移动端响应式适配（初版只考虑桌面端）
- 账号系统或云同步
- 历史数据跨设备迁移
- 任务模板/预设
- 通知推送

## 验收标准
- [ ] 双击HTML文件在浏览器中正常打开，背景雨夜窗台动画正常循环
- [ ] 窗口容器呈现矩形框+标题栏+凸起/凹陷边框质感，暖米色低饱和配色
- [ ] 计时数字使用细衬线字体（serif），清晰可读
- [ ] 可输入任务名和时长（分钟），点击添加进入任务队列
- [ ] 点击开始后按队列顺序逐任务倒计时，显示当前任务名
- [ ] 支持暂停和重置操作
- [ ] 每个任务倒计时归零时播放颂钵音效，自动切换到下一任务
- [ ] 最后一个任务完成后静默停在0:00
- [ ] 每日记录自动保存（任务名、时长、完成时间），刷新页面不丢失
- [ ] 侧边抽屉可拉出查看今日专注记录列表
- [ ] 按钮hover时有微弱暖色发光效果（低透明度box-shadow）
- [ ] 整体色彩无刺眼高饱和，视觉舒适

## 已暴露并解决的假设
| 假设 | 如何追问 | 结论 |
|---|---|---|
| 照搬Win95灰蓝色 | 反方模式挑战 | 只保留窗口结构质感，自行用暖米色重新演绎 |
| 每个任务固定时长 | 追问交互流程 | 每任务独立可调时长 |
| 记录纯后台不可见 | 追问记录可见性 | 需抽屉/侧栏可回看今日记录 |
| 可能走npm工程化 | 追问交付形式 | 单HTML文件，双击即用 |

## 技术上下文
- 纯单文件HTML（含内嵌CSS/JS）
- 背景GIF：通过base64 data URI内嵌或CDN URL引用
- 音效：颂钵音效通过 Web Audio API 或 `<audio>` 标签播放，资源通过data URI或CDN URL
- 数据存储：`window.localStorage`，key按日期组织
- 字体：浏览器默认serif（Georgia / "Times New Roman"），无需加载外部字体
- 定时器：`setInterval` 秒级精度倒计时

## 关键实体
| 实体 | 类型 | 字段 | 关系 |
|---|---|---|---|
| Task | 核心领域 | name, durationMin, status(pending/active/done) | 属于Queue |
| Queue | 核心领域 | tasks[], currentIndex | 1:many Task |
| FocusRecord | 核心领域 | taskName, durationMin, completedAt, date | 按日期聚合 |
| TimerState | 支撑对象 | remainingSeconds, isRunning, mode(idle/running/paused) | 驱动Queue |
| AudioEngine | 外部系统 | 颂钵音频资源 | 被TimerState触发 |

## 本体收敛
6轮访谈中，"计时器+任务队列+记录"三层核心实体关系稳定；Window 95 视觉标签在第4轮反方挑战中从"精确模仿"收敛为"结构质感参考"，实体模型未变化。

## 待确认问题
（全部已确认）

## 访谈记录
| 轮次 | 瞄准 | Q摘要 | A摘要 | 歧义 |
|------|------|-------|-------|------|
| 0 | 拓扑 | 6组件确认 | 对 | - |
| 1 | 约束 | 交付形式？ | 单HTML双击即用 | 55% |
| 2 | 成功标准 | 使用流程？ | 调节时长+多任务排队 | 52% |
| 3 | 约束 | GIF音效来源？ | 网搜内嵌 | 41% |
| 4 | 成功标准(反方) | Win95 vs 暖米色？ | 只保留结构质感 | 34% |
| 5 | 成功标准 | 队列终态+记录可见？ | 抽屉看今日记录 | 27% |
| 6 | 目标(简化) | 队列跑完后？ | 静默停在0:00 | 21.9%→16.1% |
